/*
    Here, we create the 'examples' schema for the test database
    if it doesn't exist - so that the test database environment
    matches the schema of the production database environment
*/
CREATE SCHEMA IF NOT EXISTS examples;